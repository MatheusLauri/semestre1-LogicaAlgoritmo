#include <stdio.h>

int main(void) {
  int x;
  printf("\t\t escreva um numero\n");
  scanf ("%d",&x);

  if (x == 14 || x == 23 || x == 24 || x == 32 || x == 33 )
  {
    printf("SENAC");
  }
  
  return 0;
}

//1 - Escreva um programa solicita um numero e exibe a mensagem "SENAC" se este número for 14, 24, 23, 32 ou 33.
